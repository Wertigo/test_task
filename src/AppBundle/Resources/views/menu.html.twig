<nav class="navbar navbar-inverse">
   <div class="container">
      <div id="navbar" class="collapse navbar-collapse">
         <ul class="nav navbar-nav">
            {% if not app.user %}
               <li><a href="{{ path('fos_user_security_login') }}">Login</a></li>
               <li><a href="{{ path('fos_user_registration_register') }}">Registration</a></li>
            {% else %}
               <li><a href="{{ path('fos_user_profile_show') }}">Profile</a></li>
                {% if app.user.isClient() %}
                    <li><a href="{{ path('create_buy_order') }}">Create buy order</a></li>
                {% elseif app.user.isSeller() %}
                    <li><a href="{{ path('create_sell_order') }}">Create sell order</a></li>
                {% endif %}
            {% endif %}
         </ul>
         {% if app.user %}
             <ul class="nav navbar-nav pull-right">
                 <li><a href="{{ path('fos_user_security_logout') }}">Logout</a></li>
             </ul>
         {% endif %}
      </div>
   </div>
</nav>